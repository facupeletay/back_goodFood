version: '3.8'
services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        APP_PORT: 3011
    container_name: nestjs_good_food_api
    working_dir: /app
    volumes:
      - .:/app
    environment:
      NODE_ENV: development
      APP_PORT: 3011
    env_file:
      - .env.development
    ports:
      - "3011:3011"
    depends_on:
      - db  # Dependencia a MySQL
    networks:
      - backend

  db:
    image: mysql:8.0
    container_name: mysql_db
    environment:
      MYSQL_ROOT_PASSWORD: rootpass  # Cambia este password
      MYSQL_DATABASE: app-genuk-test       # Base de datos por defecto
      MYSQL_USER: GENUK-TEST             # Usuario
      MYSQL_PASSWORD: Comodoro2026+         # Contraseña
    ports:
      - "3306:3306"
    networks:
      - backend
    volumes:
      - mysql_data:/var/lib/mysql    # Para persistir datos

volumes:
  mysql_data:

networks:
  backend:
    driver: bridge
